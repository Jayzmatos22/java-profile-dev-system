# 💻 Dev Profile System

Um sistema simples em **Java** para cadastro de perfis de desenvolvedores, utilizando conceitos fundamentais de Programação Orientada a Objetos (POO).

## 🚀 Funcionalidades
* Cadastro de nome e idade (Herança de `People`).
* Seleção de nível de experiência via **Enums** (`StatusDevExperience`).
* Seleção de especialidade via **Enums** (`Specialties`).
* Listagem dinâmica de linguagens de programação.
* Validação de entradas com loops `while`.
* Formatação de data atual com `LocalDate` e `DateTimeFormatter`.

## 🛠️ Tecnologias e Conceitos
* **Java 21**
* **Herança**: `Developer` estende `People`.
* **Polimorfismo**: Sobrescrita do método `toString`.
* **Encapsulamento**: Atributos privados com Getters e Setters.
* **Coleções**: Uso de `ArrayList` para gerenciar linguagens.

## 📋 Como executar
1. Compile as classes do pacote `Pessoa`.
2. Execute a classe `Main.java`.
3. Siga as instruções no console.